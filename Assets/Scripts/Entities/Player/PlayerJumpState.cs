using UnityEngine;

namespace Game
{
    public class PlayerJumpState : PlayerState
    {
        private static readonly Vector2 checkSize = Vector2.one * .1f; 
        
        private void Start()
        {
            
        }

        private void FixedUpdate()
        {
        }
    }
}
